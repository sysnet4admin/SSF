apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  # 비밀이 아닌 설정값입니다. 화면에 표시되는 메시지를 여기서 바꿉니다.
  # 비밀값(API 키 등)은 ConfigMap이 아니라 Secret으로 다룹니다(5회차).
  # 코드 기본값과 다르게 두어, 적용 즉시 화면에서 주입 여부가 보이게 합니다.
  MESSAGE: "SSF 15기 쿠버네티스 실습 (ConfigMap 적용됨)"

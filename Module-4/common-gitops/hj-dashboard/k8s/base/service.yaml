apiVersion: v1
kind: Service
metadata:
  name: hj-dashboard-svc
  labels:
    app: hj-dashboard
spec:
  type: LoadBalancer
  selector:
    app: hj-dashboard
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
